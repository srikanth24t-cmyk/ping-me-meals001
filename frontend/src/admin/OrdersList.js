import React,{useEffect,useState} from 'react';import{Link,useNavigate} from 'react-router-dom';import { adminHeaders } from '../api';export default function OrdersList(){const[orders,setOrders]=useState([]);const nav=useNavigate();async function load(){const res=await fetch((process.env.REACT_APP_API_URL||'')+'/admin/orders',{headers:adminHeaders()});if(res.status===401){localStorage.removeItem('ADMIN_TOKEN');return nav('/admin');}setOrders(await res.json());}useEffect(()=>{load();const id=setInterval(load,10000);return()=>clearInterval(id)},[]);return(<div style={{padding:20}}><h2>Orders</h2><table className='table'><thead><tr><th>ID</th><th>Phone</th><th>Status</th><th>Created</th><th>Action</th></tr></thead><tbody>{orders.map(o=>(<tr key={o.id}><td>{o.id}</td><td>{o.phone}</td><td>{o.status}</td><td>{new Date(o.created_at).toLocaleString()}</td><td><Link to={`/admin/orders/${o.id}`}>View</Link></td></tr>))}</tbody></table></div>)}